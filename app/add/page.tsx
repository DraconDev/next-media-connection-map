type Props = {};

const page = (props: Props) => {
    return (
        <div>
            <form action="post">
                <input type="text" />
                <button type="submit">Add</button>
            </form>
        </div>
    );
};

export default page;
