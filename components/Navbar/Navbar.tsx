import SearchBar from "./SearchBar";

type Props = {};

const Navbar = (props: Props) => {
    return (
        <div>
            Navbar
            <SearchBar />
        </div>
    );
};

export default Navbar;
